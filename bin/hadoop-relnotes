#!/bin/bash

new=$1

src=${HOME}/Src/apache-git/hadoop-trunk/dev-support
hrmd=${HOME}/Src/aw-github/eco-release-metadata

if [[ ! -e "${src}/releasedocmaker.py" ]]; then
  echo "no releasedocmaker.py"
  exit 1
fi

cd ${hrmd}
"${src}/releasedocmaker.py" --version "${new}" --index

